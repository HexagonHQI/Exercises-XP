const winBattle = () => true; // Arrow function winBattle

const experiencePoints = winBattle() ? 10 : 1; // operator to assign experience points

console.log(experiencePoints); // Log the experience points
